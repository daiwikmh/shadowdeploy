services:
  - type: web
    name: shadow-matcher
    runtime: docker
    dockerfilePath: sdp/ecloud/Dockerfile
    dockerContext: sdp/ecloud
    envVars:
      - key: PORT
        value: 8080
      - key: RUST_LOG
        value: shadow_matcher=info
